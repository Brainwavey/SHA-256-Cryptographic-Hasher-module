# SHA-256-Cryptographic-Hasher-module
Built an Object-Oriented cryptographic tool using Python's built-in hashlib. Translates raw user string inputs into encoded bytes and generates a 64-character hexadecimal SHA-256 hash.
